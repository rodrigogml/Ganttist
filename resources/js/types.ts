export type TaskStatus = 'completed'|'blocked'|'scheduled'|'late'|'opened'
export interface Task { id:string; title:string; kind:'task'|'section'; level:number; parent_id?:string|null; has_children?:boolean; start:string|null; finish:string|null; considered_start?:string|null; considered_deadline?:string|null; unlock_date?:string|null; earliest_start?:string|null; completed?:boolean; planned?:boolean; derived?:boolean; virtual_start?:string|null; effective_completion?:string|null; calendar_inconsistent?:boolean; sync_status?:string; progress:number; status:TaskStatus; critical:boolean; contains_critical?:boolean; total_float?:number|null; priority?:number; assignee?:string }
export interface Dependency { id:string; from:string; to:string; type:'FS'|'SS'|'FF'|'SF'; critical:boolean }
export interface Workspace { project:{id:string;name:string;source:string;sync_status:string;updated_at:string}; calendar?:{timezone:string;working_days:number[];rescheduling_mode?:'MANUAL'|'AUTOMATIC';projection_policy?:'PRESERVE_DURATION'|'PRESERVE_DEADLINE'}; tasks:Task[]; dependencies:Dependency[]; stats:{progress:number;completed:number;total:number;critical:number;opened?:number;blocked?:number;scheduled?:number;late?:number;without_dates?:number} }
